---
---

<nav>
<div class="menu-header">
<a href="/" class="site-title">
<img src="/logo2.png" alt="Logo Scartech Solution" class="logo-nav" />
</a>
<div class="menu-links">
<a href="/">Início</a>
<a href="/about">Sobre</a>
<a href="/services">Serviços</a>
<a href="/work">Portfólio</a>
<a href="/contact">Contato</a>
</div>
</div>
</nav>

<style>
nav {
z-index: 9999;
position: fixed;
top: 0;
left: 0;
right: 0;
background: linear-gradient(135deg, rgba(15, 23, 42, 0.95) 0%, rgba(30, 58, 138, 0.9) 50%, rgba(37, 99, 235, 0.85) 100%);
backdrop-filter: blur(20px);
border-bottom: 2px solid rgba(96, 165, 250, 0.4);
box-shadow: 0 4px 30px rgba(37, 99, 235, 0.3), 0 0 15px rgba(59, 130, 246, 0.2);
}

.menu-header {
display: flex;
justify-content: space-between;
align-items: center;
padding: 1rem 2rem;
max-width: 1400px;
margin: 0 auto;
}

.site-title {
  display: flex;
  align-items: center;
  text-decoration: none;
  transition: transform 0.3s ease;
}

.site-title:hover {
  transform: scale(1.05);
}

.logo-nav {
  height: 3rem;
  width: auto;
  object-fit: contain;
  filter: drop-shadow(0 0 10px rgba(37, 99, 235, 0.5));
  transition: filter 0.3s ease;
}

.logo-nav:hover {
  filter: drop-shadow(0 0 20px rgba(59, 130, 246, 0.8));
}

.menu-links {
  display: flex;
  gap: 2rem;
  align-items: center;
}

.menu-links a {
  text-decoration: none;
  color: #e0e7ff;
  font-weight: 600;
  font-size: 1rem;
  padding: 0.5rem 1rem;
  border-radius: 0.5rem;
  transition: all 0.3s ease;
  position: relative;
  text-shadow: 0 0 10px rgba(147, 197, 253, 0.3);
}

.menu-links a::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 2px;
  background: linear-gradient(90deg, #3b82f6, #2563eb);
  transition: width 0.3s ease;
}

.menu-links a:hover {
  color: #ffffff;
  text-shadow: 0 0 15px rgba(147, 197, 253, 0.8), 0 0 25px rgba(96, 165, 250, 0.5);
  background: rgba(59, 130, 246, 0.2);
}

.menu-links a:hover::after {
  width: 80%;
}

@media (max-width: 768px) {
  nav {
    padding-bottom: 0.5rem;
  }

  .menu-header {
    flex-direction: column;
    gap: 1rem;
    padding: 0.8rem 1rem;
  }
  
  .menu-links {
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
  }
  
  .menu-links a {
    font-size: 0.9rem;
    padding: 0.4rem 0.8rem;
  }
  
  .logo-nav {
    height: 2.5rem;
  }
}
</style>
